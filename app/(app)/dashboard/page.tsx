import { PagePlaceholder } from "@/components/features/PagePlaceholder";

export default function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      phase="Phase 1 — Shell"
      subtitle="High-level importer health, recent analyses, and set drafts will summarize here once data layers exist."
    />
  );
}
