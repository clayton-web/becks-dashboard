import { PagePlaceholder } from "@/components/features/PagePlaceholder";

export default function LibraryPage() {
  return (
    <PagePlaceholder
      title="Library"
      phase="Phase 4 — Playlist import"
      subtitle="Normalized tracks cached in Postgres—with duplicate detection and import provenance—will surface here."
    />
  );
}
