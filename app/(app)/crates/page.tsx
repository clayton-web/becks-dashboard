import { PagePlaceholder } from "@/components/features/PagePlaceholder";

export default function CratesPage() {
  return (
    <PagePlaceholder
      title="Crates"
      phase="Future workflow"
      subtitle="Focused shortlists for gig prep, rehearsals, or genre experiments slot in next to bulk library browsing."
    />
  );
}
